import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flexDirection: "row",
    width: "48%",
    height: 90,
    marginBottom: 15,
    borderRadius: 8,
    justifyContent: "center",
    alignItems: "center"
  },
  name: {
    fontSize: 18
  }
});
